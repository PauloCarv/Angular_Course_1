#Components 1

ng generate component course-card

html -> custom htm course-card

3 X directive


#Components 2

inputs

db-data.ts

app.component.ts -> import {COURSES} from 'db-data'

input proprty title [title]


ADD model object types

import {Course} from '../model/course'

####################################################################
##### Components Lesson 3

custom events

course-card-component.html
add event button html - (click)="onCourseViewed()"

course-card-component.ts
add event onCourseViewed with log to console

app-component.html 
add event click oncardclick

app-component.ts
oncardclick whit console log for card

app-component.html
add custom event (replace click) - (courseSelected)="onCourseSelected()"

app-component.ts
rename oncardclick to onCourseSelected

course-card-component.ts
Create event Emiter - courseSelected = new EventEmitter<Course>();
function onCourseViewed add this.courseSelected.emit(this.course); 
@output()


app-component.html
add arg $event on onCourseSelected call

app-component.ts 
ADD arg course:Course and into console also
!! add decorator output on event EventEmitter

app-component.html
add custom event (replace click) - (courseSelected)="onCourseSelected()" on remaining cards

course-card-component.ts
ADD alias to eventEmmitter
// tslint:disable-next-line: no-output-rename
  @Output('courseSelected')
  courseEmitter = new EventEmitter<Course>();


####################################################################
##### Components Lesson 4 directives

NGFOR - directive 

app-component.ts  
ADD  courses = COURSES for all courses and removed specific ones

app-component.html
ADD ngFor elem 

    <app-course-card *ngFor="let course of courses"
        (courseSelected)="onCourseSelected($event)"
        [course]="course"></app-course-card>

****add index
"let course of courses; index as i"
create new imput parameter [cardIndex]="i + 1"

course-card-component.ts 
Create new imput 

app-component.html
ADDED input index on description {{cardIndex + ' - ' +course.description}}

ADDED input first index as isFirst on ngFor

ADDED class is first [class.is-first]="true"

changed to

ADDED class is-first [class.is-first]="isFirst"

ADDED input last index as isLast on ngFor

ADDED class is-last [class.is-last]="true"
changed to
ADDED class is-last [class.is-last]="isLast"

ADDED input odd as isOdd and even isEven on ngFor
ADDED class
 [class.is-even]="isEven"
        [class.is-odd]="isOdd"


####################################################################
##### Components Lesson 5 directives

NGFIF - directive 

course-card-component.html
ADD *ngIf="true" on img elem test toggle true false
changed to *ngIf="course.iconUrl"
chenged to added function isImageVisible()

course-card-component.ts 
added function isImageVisible()

db-data.ts
Added undefined value undefined

course-card-component.html
added elvis operator
changed elvis operator with ng-if on card <div class="course-card" *ngIf="course">
added else on isImageVisible() function
added <ng-template #noimage>

